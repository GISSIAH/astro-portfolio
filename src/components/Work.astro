
---
export interface Props {
	title: string;
    techstack : string[];
    desc : string;
}

const { title , techstack, desc } = Astro.props as Props;
---


<div class="flex flex-col gap-4 p-3 max-w-sm bg-white rounded-lg border hover:shadow-xl hover:shadow-blue-200 border-gray-200 shadow-md">
    <img src={"https://picsum.photos/400/200"} class="w-full h-200" />
    <h1 class="text-2xl font-medium">
        {title}
    </h1>
    <p class="text-lg font-normal text-gray-500">
        {desc}
    </p>
    <ul class="grid grid-cols-3 gap-3 md:gap-1">
        {techstack.map(ts => (<p class="text-light border border-blue-400 rounded-xl px-2 py-1 md:text-sm md:px-1">{ts}</p>))}
    </ul>

</div>